"""Provider adapters.

A provider adapter connects the engine to one language model service.
Each adapter reports the features that its service supports.
The engine reads those features and adjusts its behavior.
"""
